# Quilt Shop 

A small repo to practice rebasing in B2


To Use:
```bash
$ git clone ...
$ cd 
$ git fetch
$ git checkout add_fabric_crud
$ git pull --rebase origin master
#fix merge conflics
$ git rebase --continue
$ git add file_name
```
